name: CodeQL

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: CodeQL disabled
        run: echo "CodeQL is disabled until GitHub Advanced Security is available."
