# Disabled until the repository is made public (CodeQL requires GitHub Advanced Security for private repos)
# name: CodeQL
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#   schedule:
#     - cron: "0 6 * * 1"
#
# jobs:
#   analyze:
#     runs-on: ubuntu-latest
#     if: ${{ !contains(github.actor, 'dependabot') }}
#     permissions:
#       security-events: write
#       contents: read
#       actions: read
#     strategy:
#       matrix:
#         language: ["python", "javascript-typescript"]
#     steps:
#       - uses: actions/checkout@v5
#
#       - uses: github/codeql-action/init@v3
#         with:
#           languages: ${{ matrix.language }}
#
#       - uses: github/codeql-action/analyze@v3
