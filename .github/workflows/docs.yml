# Disabled until the repository is made public (Deploy docs requires GitHub Advanced Security for private repos)
# name: Deploy docs

# name: Deploy docs

# on:
#   push:
#     branches: [main]
#     paths:
#       - "docs/**"
#       - "mkdocs.yml"
#       - ".github/workflows/docs.yml"
#   workflow_dispatch: {}

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# concurrency:
#   group: pages
#   cancel-in-progress: false

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v5

#       - uses: actions/setup-python@v6
#         with:
#           python-version: "3.12"

#       - name: Install MkDocs Material
#         run: pip install -r docs/requirements.txt

#       - name: Build site
#         run: mkdocs build --strict

#       - uses: actions/upload-pages-artifact@v3
#         with:
#           path: site

#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     steps:
#       - id: deployment
#         uses: actions/deploy-pages@v4
