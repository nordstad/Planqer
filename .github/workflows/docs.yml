name: Deploy docs

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Docs deployment disabled
        run: echo "Docs deployment is disabled until the repository is public."
